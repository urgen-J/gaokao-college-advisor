# 把这个网站上线（方式 A：Railway / Render 海外自动部署）

> 这是一份给"不懂代码"的朋友看的大白话说明书。照着点就行，不用理解原理。

---

## 一、上线前你只需要做两件事

1. **把代码传到 GitHub**（新建一个仓库，把整个项目文件夹传上去）。
   - ⚠️ 你的 `.env` 文件里有 DeepSeek / 通义的密钥，**已经被设置成不会上传**，放心传。
   - 上传后，密钥要在平台的网页里单独填（见下文），千万别把密钥直接写进代码。

2. **准备好两个"钥匙"（API Key）**，上线时填到平台里：
   - **DeepSeek 密钥（必须填）**：去 https://platform.deepseek.com 注册 → 充值几块钱 → 在"API Keys"里新建一个，复制下来。
   - **通义千问密钥（可选）**：只有你想开"联网搜索"才需要，去 https://dashscope.console.aliyun.com 申请 `QWEN_API_KEY`。不填也能用，只是不能联网搜最新信息。

---

## 二、选一个平台（二选一，都免费起步）

### 方案 1：Railway（最省事，推荐）

1. 打开 https://railway.app ，用 GitHub 账号登录。
2. 点 **"New Project" → "Deploy from GitHub repo"**，选你刚传的仓库。
3. Railway 会自动识别这是 Node 项目，用我们配好的 `railway.json`：
   - 构建命令：`npm run build`（自动打包网页）
   - 启动命令：`npm start`（自动跑后台）
4. 进入项目的 **"Variables"** 标签，点 **"New Variable"**，挨个填入：
   - `CODEBUDDY_API_KEY` = 你的 DeepSeek 密钥
   - `CODEBUDDY_BASE_URL` = `https://api.deepseek.com`（照抄）
   - `QWEN_API_KEY` = 你的通义密钥（没有就留空或不填）
   - `LOGIN_USERNAME` = 后台登录账号（例如 `admin`）
   - `LOGIN_PASSWORD` = 后台登录密码（**一定要改成你自己的，别用默认 admin123**）
5. 等它自己构建完（几分钟），点顶部的网址链接，就能打开你的网站了。
6. 以后你改了代码，只要 `git push` 一下，Railway 会自动重新上线。

### 方案 2：Render

1. 打开 https://render.com ，用 GitHub 登录。
2. 点 **"New" → "Web Service"**，选你的仓库。
3. 设置（项目里已写好 `render.yaml`，大部分会自动填好，你核对一下）：
   - **Build Command**：`npm install && npm run build`
   - **Start Command**：`npm start`
   - **Instance Type**：先选 Free（免费，但一段时间没人访问会睡，醒来要等几秒；想常驻选付费）
4. 往下拉到 **"Environment" → "Add Environment Variable"**，填入和上面 Railway 一样的 5 个变量。
5. 点 **"Create Web Service"**，等部署完成，点生成的网址即可访问。
6. 同样，之后 `git push` 会自动更新。

---

## 三、上线后怎么用

- 浏览器打开平台给你的网址 → 用你设的 `LOGIN_USERNAME` / `LOGIN_PASSWORD` 登录 → 就能聊天了。
- 网页和后台是"同一个网址"，不会出现跨域报错。

---

## 四、必须知道的 5 个注意事项

1. **密钥别泄露**：只填在平台的 Variables 里，别写进代码、别发到群里。
2. **对话记录存在服务器本地文件**：免费版重启可能清空；要长期保存，以后可换数据库或挂持久磁盘（先不用管）。
3. **登录态重启会掉**：服务器重启后所有人需要重新登录（已知小限制，单台服务器不影响使用）。
4. **国内访问可能偏慢**：服务器在海外，国内打开偶尔慢一点；要快就走方式 B（国内云服务器）。
5. **后台账号默认 admin/admin123 必须改**：否则别人能登进你的后台。

---

## 五、常见问题

- **打开网页是空白 / 一直在转？** 多半是 `CODEBUDDY_API_KEY` 没填或填错，去平台 Variables 核对后重新部署。
- **能登录但发消息没回复？** 检查 DeepSeek 密钥是否有效、账户是否有余额；看平台日志里的报错。
- **部署失败？** 看平台构建日志，最常见的就是依赖装不上或端口没读 `PORT`（本项目已正确处理 `PORT`）。

---

## 附：本项目为上线做的准备（已就绪）

- `package.json`：`build` = 只打包前端；`start` = 跑后台；`tsx` 已放进生产依赖。
- `server/index.ts`：检测到 `dist/` 时就由后台直接托管网页，无需额外配置反向代理。
- `railway.json` / `render.yaml` / `Procfile`：平台识别与启动配置。
- `.gitignore`：已忽略 `.env`、`node_modules`、`dist`、`data`，密钥和垃圾不会进仓库。
- 临时调试接口 `/api/_env` 已删除，不会泄露密钥配置。
